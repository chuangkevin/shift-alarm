# 0.3.14 第二台狀態

- 第二台 USB MAC `fc:01:2c:c9:9c:a8` 已寫入 0.3.14 app0，映像版本及寫後 hash 通過。
- 保留 Wi-Fi profiles、Tailnet 身分與其他 NVS；第一台未操作。
- 將四個高流量網路 tag 設為 WARN，逐封包 WG RX 與 periodic retry 改用預設關閉的 WG_DEBUG；沒有改動協定、鎖、priority 或重試策略。
- 改前 35 秒 USB 輸出 156,662 bytes；改後 35 秒 22,603 bytes。兩個樣本網路負載未控制，因此只能證明輸出量下降，不能等同按鍵延遲改善。
- 改後三筆 STATUS 均為 Wi-Fi=1、clock=1，未觀察到 panic／watchdog。
- 74 個 Python tests、microlink host tests 與 ESP-IDF 5.3.2 build 通過。
- 實體按鍵改善尚待使用者確認。DISCO 計算、lwIP lock 與主迴圈 HTTP handler 仍可能影響延遲。
