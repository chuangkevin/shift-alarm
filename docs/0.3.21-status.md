# 0.3.21 後端修正與 Tailscale OTA 實測

日期：2026-09-15（臺北時間）。

- 裝置：`shiftalarm-9ca8`，MAC `fc:01:2c:c9:9c:a8`，Tailnet `100.104.66.47`。
- 裝置固定後端：GN100 `100.127.82.47:8237`，Caddy 經 Tailnet 代理到 rpi-matrix `100.126.226.79:8237`。
- 修正 MicroLink 只安裝前 48 個 peers 時可能漏掉後端的問題：priority peer 現在先安裝；DERP preferred region 為香港 20。
- GN100 `shift-alarm-device-wake.timer` 每 30 秒讀裝置狀態，協助重開後建立 WireGuard session。
- 修正已 provision 裝置仍沿用舊 NVS device token而造成 heartbeat 401；只有 token 值不同才覆寫並讀回驗證。
- 0.3.21 私有 app 映像為 1,653,056 bytes，SHA-256 `e64bf2fa40832e083aa8660f4ecc66bf9e5d8a6dc518d7784ef712636a6839f0`。
- 由 0.3.20 執行兩階段 OTA：download 完整 staged 並驗證版本，另一次 install 切換、重開及回連成功。
- 重開後驗收：版本 0.3.21、後端可達、健康檢查成功、5 個鬧鐘、班表已同步、方向 90°、亮度 25%、關屏 5 分鐘、Wi-Fi 與 Tailnet 身分保留；更新狀態為 idle。
- 測試：Python 76 passed；alarm_ota host tests、alarm_proxy framing/stream tests、ESP-IDF build 通過。

第一次 install POST 在 HTTP timeout 前未被接受。確認裝置仍為 0.3.20、staged metadata 與 sequence 未變、`canInstall=true` 後才人工送出第二次。這是人工根據唯讀證據作出的決定，不可改成工具自動重試。
